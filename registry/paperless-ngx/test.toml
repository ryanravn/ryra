[[steps]]
action = "add"
service = "paperless-ngx"

[[steps]]
action = "wait"
service = "paperless-ngx"

[[steps]]
action = "http"
name = "paperless-ngx-responds"
url = "http://127.0.0.1:$RYRA_PORT_HTTP"
service = "paperless-ngx"
poll = { interval = 2, attempts = 30 }
timeout = 90

[[steps]]
action = "shell"
name = "data-dir-created"
run = "test -d $HOME/.local/share/ryra/paperless-ngx"
timeout = 10
