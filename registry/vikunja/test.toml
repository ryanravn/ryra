[[steps]]
action = "add"
service = "vikunja"

[[steps]]
action = "wait"
service = "vikunja"

[[steps]]
action = "http"
name = "vikunja-responds"
url = "http://127.0.0.1:$RYRA_PORT_HTTP"
service = "vikunja"
poll = { interval = 2, attempts = 30 }
timeout = 90

[[steps]]
action = "shell"
name = "data-dir-created"
run = "test -d $HOME/.local/share/ryra/vikunja"
timeout = 10
