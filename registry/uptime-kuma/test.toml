[setup]
services = ["uptime-kuma"]

[[tests]]
name = "http-responds"
run = "for i in $(seq 1 20); do curl -sf http://127.0.0.1:$RYRA_PORT_HTTP && exit 0; sleep 2; done; exit 1"
timeout = 60

[[tests]]
name = "data-dir-created"
run = "test -d $HOME/.local/share/ryra/uptime-kuma"
