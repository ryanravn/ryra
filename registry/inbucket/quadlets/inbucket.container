[Unit]
Description=Inbucket
StartLimitIntervalSec=60
StartLimitBurst=3

[Container]
ContainerName=inbucket
Image=docker.io/inbucket/inbucket:3.1.1
Network=inbucket.network
PublishPort=${PORT_HTTP}:9000
PublishPort=${PORT_SMTP}:2500
Pull=missing
EnvironmentFile=%h/services/inbucket/.env
HealthCmd=curl -sf http://localhost:9000/status || exit 1

[Service]
EnvironmentFile=%h/services/inbucket/.env
Restart=always
RestartSec=5
TimeoutStartSec=300

[Install]
WantedBy=default.target
